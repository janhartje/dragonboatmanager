import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log('User count:', await prisma.user.count()); } main().catch(console.error).finally(() => prisma.$disconnect());
